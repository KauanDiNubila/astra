/**
 * Shared module — cross-cutting infrastructure.
 *
 * Configuration, security, exception handling and base types used by
 * every feature module. Contains no business rules of its own.
 */
package com.astra.shared;
