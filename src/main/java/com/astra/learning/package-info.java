/**
 * Learning module — Course, CourseModule and Goal.
 *
 * Progress = completed modules / total modules. Granularity is the
 * module level, not the lesson. NOTE: the course-module entity is named
 * CourseModule (mapped to table "module") to avoid clashing with
 * java.lang.Module; there is deliberately no package named "module".
 */
package com.astra.learning;
